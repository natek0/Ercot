# Core dependencies (pinned to the versions Stage 0 was developed and validated on).
# Python 3.12.

# Optimization: perfect-foresight LP + duals
cvxpy==1.9.2
highspy==1.15.1          # HiGHS solver backend

# Numerics / data
numpy==2.5.1
pandas==2.3.3
pyarrow==25.0.0          # parquet cache

# ERCOT ingest
requests==2.34.2
python-dotenv==1.2.2     # loads .env credentials
python-calamine==0.8.2   # reads ERCOT's non-standard .xlsx MCPC bundles
openpyxl==3.1.5

# Added in Stage 1 (SQL warehouse + tests/CI)
duckdb==1.5.4            # SQL warehouse + feature views
pytest==9.1.1            # verification suite as tests (run in CI)

# Added in Stage 3 (learned conditional price distribution)
scikit-learn==1.7.2      # gradient-boosted-tree quantile regression (pinball loss),
                         # isotonic rearrangement; prebuilt wheel, needs only numpy/scipy

# Added in Stage 5 (statistical inference + figures)
scipy==1.18.0            # exact binomial sign test, normal quantiles (power statement)
matplotlib==3.11.1       # publication figures (value-of-foresight ladder, duration curves,
                         # concentration, bootstrap CI, psi_up tail, sign-test histogram)
